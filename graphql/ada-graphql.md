> **NOT A PUBLISHED ADA CONTRACT.** Ada publishes no GraphQL API. Verified 2026-08-14: no
> `/graphql` endpoint on any Ada host, no GraphQL surface in docs.ada.cx, and no GraphQL entry
> in Ada's RFC 9727 api-catalog. The accompanying `ada-schema.graphql` is a speculative,
> model-authored sketch, not something Ada operates. The `type: GraphQL` pointers that used to
> reference these files were removed from apis.yml on 2026-08-14.

# Ada GraphQL API

Ada is an AI-powered customer service platform. The API covers bot configuration, conversation management, intents, responses, analytics, handoff to live agents, and multi-channel deployment for automated customer support.

**Endpoint:** No native GraphQL endpoint — conceptual schema derived from Ada REST API
**Documentation:** https://docs.ada.cx/reference/introduction/overview

- Reference: https://docs.ada.cx/reference/introduction/overview
- Schema: https://github.com/api-evangelist/ada
